import { Controller } from '@nestjs/common';

@Controller('users')
export class UsersController {
    // GET /users
    // GET /users/:id
    // POST /users/:id
    // PATCH /users/:id
    // DELETE /users/:id 
}
